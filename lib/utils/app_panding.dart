import 'package:flutter/material.dart';

/// widget, consider using [kPadding] to obtain these values
const kPaddingTen = EdgeInsets.all(10.0);
const kSymPaddingTen = EdgeInsets.symmetric(horizontal: 10.0, vertical: 10.0);
const kVertical16PAdding = EdgeInsets.symmetric(vertical: 16.0);
const kAll16PAdding = EdgeInsets.all(16.0);
